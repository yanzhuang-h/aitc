# 路口控制函数化与大模型调用说明

**路口控制函数化与大模型调用说明**

适用版本：aitc\-mian

能够执行本项目 Python 代码的 Agent 可以按本文接口调用这些函数。

**一、总体调用链**

单路口初始方案（DQN/经验表/时刻表）
  \-\> Internet 在线数据聚合
  \-\> Internet 路口处理
  \-\> mixed 路口处理
  \-\> flow 路口处理
  \-\> 特殊路口及路段联动调整
  \-\> 最小周期补齐
  \-\> 可选绿波协调
  \-\> 整数化与浮动值应用
  \-\> 最终控制方案


完整非绿波调用建议使用 process\_all\_intersections\(\)。现有 Server\_AITC 仍通过 coordinate\(\) 兼容入口运行，默认包含绿波。三类路口集合可能重叠，因此不能按单一类别覆盖一个路口。

**二、统一数据格式**

|**字段/下标**|**类型**|**说明**|
|---|---|---|
|plans|dict\[str, list\]|路口编号到十元素方案的映射；全局函数通常原地修改|
|plan\[0:8\]|number|各相位绿灯时间，单位为秒；具体方向由 road\_info\.json 的 phase 配置决定|
|plan\[8\]|number|历史兼容保留位|
|plan\[9\]|number|当前方案/状态编号|
|coordinate\_data|dict|路口相位起点信息，常见字段为 s1、s2|
|online\_data|dict|按路段、时间组织的在线观测|
|extend\_data|dict or None|实际执行阶段或延长数据|



**三、可调用模块**

|**函数**|**作用**|**输入**|**输出**|
|---|---|---|---|
|generate\_intersection\_plan\(request\)|调用原 DQN\_select 生成单路口初始方案|IntersectionControlRequest|ControlResult；成功含十元素 plan，失败返回全零方案和 error|
|get\_timetable\_plan\(cross\_id, target\_time\)|读取异常路径的工作日/周末当前小时方案|路口编号、datetime/Unix 时间，可选运行时时刻表|ControlResult；source 标记方案来源|
|process\_internet\_intersections\(\.\.\.\)|处理 intern\_road\_id 类别并执行公共后处理|plans、coordinate\_data、online\_data、overflow\_data、extend\_data|更新后的全局方案字典；绿波关闭|
|process\_mixed\_intersections\(\.\.\.\)|处理 shipin\_road 类别|plans、coordinate\_data、online\_data、overflow\_data、extend\_data|更新后的全局方案字典；绿波关闭|
|process\_flow\_intersections\(\.\.\.\)|处理 shipin1\_road 类别|plans、coordinate\_data、online\_data、overflow\_data、extend\_data|更新后的全局方案字典；绿波关闭|
|process\_all\_intersections\(\.\.\.\)|按 Internet \-\> mixed \-\> flow 顺序处理完整快照|plans、coordinate\_data、online\_data、overflow\_data、extend\_data|最终非绿波全局方案字典；推荐入口|



**四、内部处理模块**

|**模块/函数**|**作用**|**输入**|**输出**|
|---|---|---|---|
|update\_internet\_road\_state|将路段观测聚合到路口 L/R/U/D 状态|state、online\_data\_map、路口集合、方向映射|同一个 state；每方向五个历史槽位|
|calculate\_internet\_direction\_demand|计算 Internet 方向需求和拥堵增量|方向状态、小时基础值、Init\_add 回调|L/R/U/D 需求字典|
|select\_internet\_control\_state|按方向差、组合拥堵和小时规则选状态|路口、需求、road\_config、小时|状态编号字符串|
|apply\_internet\_phase\_demand|把方向需求写入八个相位|方案、phase 类型、权重、需求|原地更新方案|
|process\_internet\_intersection|组合上述步骤处理一个 Internet 路口|路口状态、基础值、配置、强制状态回调|\(plan, report\)|
|calculate\_mixed\_direction\_demand|将当前视频方案和 Internet 增量合并|方向状态、当前方案、Init\_add|八方向需求字典|
|select\_mixed\_control\_state / generate\_mixed\_phase\_plan|选择 mixed 状态并生成有上下限的相位|方向状态、需求、配置、小时|状态编号 / 原地更新方案|
|process\_mixed\_intersection|组合 mixed 的时刻表回退、状态和相位步骤|路口、当前方案、方向状态、配置、回调|\(plan, report\)|
|select\_flow\_control\_state / generate\_flow\_phase\_plan|选择 flow 状态并生成普通/半相位/转向相位|路口、需求、配置、小时|状态编号 / 原地更新方案|
|process\_flow\_intersection|组合 flow 的完整单路口处理|路口、当前方案、配置、小时、回调|\(plan, report\)|
|apply\_special\_intersection\_adjustments|执行三类处理之后、绿波之前的特殊路口及路段联动规则|全局方案、在线状态、路口映射、协调数据、时刻表回调|原地更新后的全局方案|
|complete\_minimum\_cycle|把前五相位总和补到至少 60 秒|全局方案字典|原地更新后的方案字典|
|finalize\_plan\_values|整数化后应用工作日/时段浮动值|全局方案字典|\(方案字典, 浮动值报告\)|



**五、模块调用契约**

模型调用时应优先使用稳定入口，不要直接调用带有 road\_info、se\_map 等内部全局依赖的特殊调整函数。模型负责提供结构化输入，函数负责计算方案；模型不应自行改写 plan\[0:8\] 的业务含义。

所有失败结果都应检查 success 或 report\.success。单路口函数通常返回 \(plan, report\)，全局函数返回方案字典；全局函数为兼容旧系统可能跳过格式异常记录，因此调用方应保留日志和快照。

**六、路口 1300069 示例**

**6\.1 单路口 DQN 输入**

\{
  "cross\_id": "1300069",
  "current\_time": 1786435800,
  "traffic\_vector": \[\],
  "queue\_vector": \[\],
  "traffic\_vector\_duration2": \[\],
  "flow\_map": \{"2026\-08\-11 16:10:00": \{"U": 32, "D": 18, "L": 12, "R": 9\}\},
  "queue\_map": \{\},
  "stage\_map": \{\},
  "previous\_coordinate": \{\},
  "predicted\_flow": \{\},
  "predicted\_queue": \{\},
  "extend\_map": \{\},
  "overflow\_map": \{\},
  "radar\_map": \{\},
  "boyan\_map": \{\}
\}


上面是模型工具层看到的结构化请求。空列表或空字典表示该数据源本轮没有数据，不表示路口不存在。实际调用时 current\_time 应使用当前 Unix 秒时间戳。

**6\.2 单路口输出**

\{
  "success": true,
  "cross\_id": "1300069",
  "plan": \[49, 30, 53, 21, 0, 0, 0, 0, 0, 0\],
  "source": "dqn\_selector",
  "coordinate\_data": \{\},
  "model\_info": \{\},
  "experience\_info": \{\},
  "warnings": \[\],
  "error": null
\}


|**输出位置**|**本例含义**|
|---|---|
|plan\[0\] = 49|1300069 的 phase\[0\] 对应相位绿灯 49 秒|
|plan\[1\] = 30|phase\[1\] 对应相位绿灯 30 秒|
|plan\[2\] = 53|phase\[2\] 对应相位绿灯 53 秒|
|plan\[3\] = 21|phase\[3\] 对应相位绿灯 21 秒|
|plan\[4:8\] = 0|本方案中这些相位没有分配绿灯|
|plan\[9\] = 0|本例选中的状态编号为 0；最终含义以 road\_info\.json 为准|



**6\.3 全局入口输入与输出**

result = process\_all\_intersections\(
    plans=\{"1300069": \[49, 30, 53, 21, 0, 0, 0, 0, 0, 0\]\},
    coordinate\_data=\{"1300069": \{"s1": 0, "s2": 0\}\},
    online\_data=\{\},
    overflow\_data=\{\},
    extend\_data=\{\},
\)


全局入口需要传入完整快照，而不是只传 1300069。因为特殊路口和路段联动规则会读取其他路口，例如 1300068、1300106、2703062 等。简化示例只展示调用形状；生产调用应传入 Server\_AITC 快照中的完整 plans、coordinate\_data、online\_data、overflow\_data 和 extend\_data。

在真实快照 20260811\_154211\_859531\.json 中，兼容 coordinate\(\.\.\., include\_green\_wave=False\) 和 process\_all\_intersections\(\.\.\.\) 对 1300069 以及全部其他路口的输出完全一致，差异数为 0。

**七、当前是否已经是大模型自动调用**

当前状态是“函数已经工具化准备完成”，不是远程大模型自动调度。能执行本项目 Python 的 Agent 可以调用函数注册表：

from lib\.control\_functions import get\_control\_function

fn = get\_control\_function\("process\_all\_intersections"\)
result = fn\(plans, coordinate\_data, online\_data, overflow\_data, extend\_data\)

若要接入 OpenAI API 或其他模型平台，还需要在外围增加 JSON Schema、工具选择循环、参数校验、权限/超时、调用审计和结果回传。本文档描述的是模型与 Python 函数之间的业务契约。

**八、验证记录**

|**验证项**|**结果**|
|---|---|
|Python 语法检查|通过|
|控制函数单元测试|30 项全部通过|
|真实控制快照|control\_20260811\_154211\_859531\.json|
|快照等价结果|equal: true；difference\_count: 0|
|绿波边界|公开全局函数关闭绿波；兼容 coordinate 默认保留绿波|

