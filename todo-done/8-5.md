# aitc

敏捷开发、快速迭代、先完成再完美

## 1 大模型底座开发

快速部署上架，实现简单的部署、开发，实现一个最基本的功能用以展示。

千问控制大模型：
我们先实现模型简单的部署，基础的日常问答，之后开发工具，进行agent开发：

千问Agent 能调用几个函数，输出到哪里
这一块拥有的功能：
（1）从数据底座接口取数据的tool
（2）处理数据的tool
（3）展示处理结果的tool

初步实现（2026年/8/1-2026/8/15）：
实现功能：
（1）要是实在赶不上，就实现一个调用单点路口函数的功能就行，后续全部用算法写死即可。


## 2 数据底座开发

目标：将现有的数据收发、运行缓存、数据聚合、配置、经验池等能力逐步从 `Server_AITC.py` 和旧模块中剥离，沉淀到 `infra/data` 数据底座中。后续 Agent 或其他服务可以通过数据底座接口获取数据，例如千问主控大模型查询路口数据、调用 RAG、查询经验池等。

### 2.1 数据范围

- [ ] 路口车流数据：视频感知流量数据、排队数据、信号阶段数据。
- [ ] 雷达/博研数据：雷达普通数据、雷达事件、博研设备数据。
- [ ] 互联网/外部扩展数据：online、latest、extend。
- [ ] 溢出告警数据：overflow warning。
- [ ] 配置项：road_info、cross_info、road_state、intersection_result_config、floating_value。
- [ ] 经验池：DQN/算法生成的 EXP 数据。
- [ ] 时段方案：time_schedule、内外部时段表、每日更新结果。

### 2.2 核心能力

- [x] 研读旧源码中的数据流，确认 `Server_AITC.py` 当前混合了 TCP 接收、HTTP 雷达接收、配置接口、窗口缓存、周期聚合、DQN 调用、结果打包和广播发送。
- [x] 建立 `infra/data` 数据底座目录。
- [x] 建立运行数据分类组件：`infra/data/classifier.py`。
- [x] 建立运行窗口缓存组件：`infra/data/runtime_cache.py`。
- [x] 建立运行数据写入门面：`infra/data/writer.py`，当前先兼容旧 `Write_to_file.py`。
- [x] 建立旧聚合适配组件：`infra/data/legacy_processor.py`，当前先兼容旧 `Process_cache_data.py`。
- [x] 将 TCP/HTTP 接收后的分类、写入、窗口缓存、雷达事件状态和溢出告警状态更新合并到 `infra/data/receiver.py`。
- [x] 将 `Server_AITC.py` 的旧全局 deque 窗口迁移到 `RuntimeDataCache`。
- [x] 将 `process_data_to_send()` 的缓存快照读取和旧聚合函数调用接入 `LegacyCacheProcessor`。
- [x] 将预测数据写入能力从 `Write_to_file.py` 逐步收口到 `RuntimeDataWriter`，包括 flow_pre、queue_pre、phase_check、send、EXP。
- [ ] 将 `Server_AITC.py` 中的周期处理流程继续瘦身，只保留调度、算法调用和结果广播。
- [ ] 将 HTTP 配置接口从 `Server_AITC.py` 中拆出，迁移到数据底座配置模块。
- [ ] 将 `lib/config_api.py`、`lib/*_functions.py`、`lib/intersection_config.py`、`lib/road_state.py` 逐步纳入 `infra/data` 的配置持久化边界。
- [ ] 将 Nacos 同步逻辑纳入数据底座配置同步模块。
- [ ] 将 `time_schedule.py` 相关能力纳入数据底座的时段方案/经验池更新模块。
- [ ] 为 Agent 提供查询数据底座的工具接口，例如查询路口近期流量、排队、雷达、配置和经验池。
- [ ] 补充数据底座单元测试和集成测试。

### 2.3 当前已完成

- [x] 保持算法不变，优先完成组件边界抽象。
- [x] `Server_AITC.py` 不再直接导入 `Process_cache_data.py`。
- [x] `Server_AITC.py` 的数据接收入口已变薄：
  - [x] TCP 普通数据入口调用 `runtime_data_receiver.receive_tcp(item)`。
  - [x] HTTP 雷达/博研入口调用 `runtime_data_receiver.receive_http(item)`。
- [x] 运行窗口由 `RuntimeDataCache` 统一管理。
- [x] 旧算法仍接收原来的数据形态，降低迁移风险。

### 2.4 下一步计划

- [x] 第一步：将 `Write_to_file.py` 中与运行结果相关的写入继续收口到 `RuntimeDataWriter`。
  - [x] `write_to_flow_predict_file`
  - [x] `write_to_queue_predict_file`
  - [x] `write_to_phase_check_file`
  - [x] `write_to_send_file`
  - [x] `gen_EXP_Json`
- [ ] 第二步：抽象结果输出边界，将广播前的结果写入和发送记录从 `Server_AITC.py` 中移出。
- [ ] 第三步：梳理配置接口，设计 `infra/data/config` 或等价模块。
- [ ] 第四步：梳理 `time_schedule.py`，确认哪些属于经验池更新，哪些属于配置服务。
- [ ] 第五步：为大模型 Agent 提供第一批数据查询工具。

### 2.5 扩展性要求

- [ ] 新增数据类型时，只需要扩展分类规则、缓存策略和处理适配。
- [ ] 新增数据来源时，优先扩展 receiver，不直接改算法。
- [ ] 新增接口时，优先通过数据底座暴露，不让 Agent 直接读取旧文件。
- [ ] 运行数据、缓存、日志、模型文件不提交到 Git。

数据底座最终应该专注做数据的接收、分类、缓存、持久化、聚合、查询和对外提供接口。Agent、算法和服务入口都应通过数据底座获取数据，而不是直接读写散落的旧文件。

